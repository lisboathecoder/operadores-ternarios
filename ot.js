let status;
let estoque = 0;

// Usando if-else
if(estoque > 0) {
status = 'Disponível';
} else {
    status = 'Indisponível';
}

console.log(status);

// Usando operador ternário
let estoque2 = 10;
b
const status2 = estoque2 > 0 ? 'Disponível' : 'Indisponível';

console.log(status2);